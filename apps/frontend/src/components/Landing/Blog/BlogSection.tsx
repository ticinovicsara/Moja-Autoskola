import { SectionWrapper } from "@/components/SectionWrapper/SectionWrapper";
import BlogSlider from "./BlogSlider/BlogSlider";

export const BlogSection = () => {
  return (
    <SectionWrapper title="Baci oko na naše blogove!">
      <BlogSlider />
    </SectionWrapper>
  );
};
